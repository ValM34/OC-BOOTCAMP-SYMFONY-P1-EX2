{% extends 'public.html.twig' %}

{% block title %}Bienvenue sur TaskLinter{% endblock %}

{% block body %}
  <h1 class="home-title">Bienvenue sur TaskLinker</h1>
  <p class="home-subtitle">Commencez dès maintenant - gratuitement</p>
  <p>Vous avez déjà un compte ?</p>
  <div class="button-register-container">
    <a href="#" class="button button-submit">Se connecter</a>
    {# <a href="{{ path('app_login') }}" class="button button-submit">Se connecter</a> #}
  </div>
  <div class="container-home-separator">
    <div class="home-separator"></div>
    <div>ou</div>
    <div class="home-separator"></div>
  </div>
  <div class="button-register-container">
    <a href="{{ path('app_register') }}" class="button button-submit button-register">S'inscrire</a>
  </div>
    
{% endblock %}
